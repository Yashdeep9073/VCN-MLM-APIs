info:
  name: Placement Options
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_USER_URL}}holding-tank/read/placement-options"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
