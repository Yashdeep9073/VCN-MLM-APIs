info:
  name: Full Auto Placement Update
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{LOCAL_USER_URL}}holding-tank/update/place"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"holdingId\": 18,\r

      \    \"userId\": 19,\r

      \    \"forcedPlacement\": true\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
