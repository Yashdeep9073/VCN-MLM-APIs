info:
  name: MLM Tree User
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_USER_URL}}holding-tank/read/mlm-tree-nodes"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
