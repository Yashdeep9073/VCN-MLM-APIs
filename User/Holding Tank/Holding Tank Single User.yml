info:
  name: Holding Tank Single User
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_USER_URL}}holding-tank/read/user-id/1"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
