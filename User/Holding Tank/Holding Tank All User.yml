info:
  name: Holding Tank All User
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_USER_URL}}holding-tank/read"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
