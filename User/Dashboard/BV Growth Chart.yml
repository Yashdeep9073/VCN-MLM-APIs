info:
  name: BV Growth Chart
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_USER_URL}}dashboard/read/bv-growth"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
