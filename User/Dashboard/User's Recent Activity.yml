info:
  name: User's Recent Activity
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_USER_URL}}dashboard/read/recent-activity"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
