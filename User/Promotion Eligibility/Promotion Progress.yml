info:
  name: Promotion Progress
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_USER_URL}}promotion-eligibility/read/progress"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
