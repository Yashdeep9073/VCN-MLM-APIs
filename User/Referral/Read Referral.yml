info:
  name: Read Referral
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_USER_URL}}referral/read"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
