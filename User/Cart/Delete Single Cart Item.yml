info:
  name: Delete Single Cart Item
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{LOCAL_USER_URL}}cart/delete/item/1"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
