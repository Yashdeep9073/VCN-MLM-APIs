info:
  name: View Payout Preference
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_USER_URL}}wallet/read/payout-preference"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
