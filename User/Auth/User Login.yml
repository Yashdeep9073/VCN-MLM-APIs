info:
  name: User Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{LOCAL_USER_URL}}auth/login"
  body:
    type: json
    data: |-
      {
          "identifier":"yolanda1012",
          "password":"yolanda1012"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
