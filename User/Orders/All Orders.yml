info:
  name: All Orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_USER_URL}}orders/read"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
