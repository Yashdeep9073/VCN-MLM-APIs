info:
  name: Single Order
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_USER_URL}}orders/read/1"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  params:
    - name: ""
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
