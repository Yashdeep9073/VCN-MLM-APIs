info:
  name: Update Profile
  type: http
  seq: 2

http:
  method: PUT
  url: "{{LOCAL_USER_URL}}profile/update"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"email\":\"root@vcn.in\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
