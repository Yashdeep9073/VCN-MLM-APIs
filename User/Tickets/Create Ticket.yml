info:
  name: Create Ticket
  type: http
  seq: 1

http:
  method: POST
  url: "{{LOCAL_USER_URL}}tickets/create"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"subject\":\"need to solve this \",\r

      \    \"priority\":\"LOW\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
