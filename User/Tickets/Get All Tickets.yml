info:
  name: Get All Tickets
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_USER_URL}}tickets/read"
  headers:
    - name: Authorization
      value: "{{USER_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
