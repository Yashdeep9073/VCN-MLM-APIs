info:
  name: Subscribe
  type: http
  seq: 1

http:
  method: POST
  url: "{{LOCAL_COMMON_URL}}subscribe/create"
  body:
    type: json
    data: "{\r

      \    \"email\":\"test@gmail.com\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
