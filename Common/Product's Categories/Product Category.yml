info:
  name: Product Category
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_COMMON_URL}}product-category/read?limit=2"
  params:
    - name: limit
      value: "2"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
