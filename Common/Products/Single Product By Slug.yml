info:
  name: Single Product By Slug
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_COMMON_URL}}product/read/slug/vitamin-c-capsules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
