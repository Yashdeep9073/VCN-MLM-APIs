info:
  name: Single Product
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_COMMON_URL}}product/read?search=Vitamin C Capsules"
  params:
    - name: search
      value: Vitamin C Capsules
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
