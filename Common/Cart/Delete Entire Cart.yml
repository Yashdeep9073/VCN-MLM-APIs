info:
  name: Delete Entire Cart
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{SERVER_COMMON_URL}}cart/delete/clear?sessionId=1"
  params:
    - name: sessionId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
