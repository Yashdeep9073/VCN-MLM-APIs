info:
  name: Update Cart Items
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{SERVER_COMMON_URL}}cart/update"
  body:
    type: json
    data: "{\r

      \    \"cartItemId\":2,\r

      \    \"quantity\":1\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
