info:
  name: Get Cart Items
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_COMMON_URL}}cart/read?sessionId=1"
  params:
    - name: sessionId
      value: "1"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
