info:
  name: Cart
  type: http
  seq: 1

http:
  method: POST
  url: "{{SERVER_COMMON_URL}}cart/create"
  body:
    type: json
    data: "{\r

      \    \"variantId\": 40,\r

      \    \"quantity\":1,\r

      \    \"sessionId\":\"1\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
