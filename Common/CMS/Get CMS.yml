info:
  name: Get CMS
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_COMMON_URL}}cms-pages/read?productId=4"
  params:
    - name: productId
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
