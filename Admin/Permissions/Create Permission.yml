info:
  name: Create Permission
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}permissions/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"name\":\"Manage Products\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
