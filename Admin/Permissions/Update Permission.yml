info:
  name: Update Permission
  type: http
  seq: 6

http:
  method: PUT
  url: "{{SERVER_ADMIN_URL}}permissions/update"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"id\": 5,\r

      \    \"name\": \"Dashboard Stats\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
