info:
  name: Create Advisor  Income Components
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}advisor-income-components/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "definitionId": 1,
        "walletId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
