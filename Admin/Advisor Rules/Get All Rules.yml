info:
  name: Get All Rules
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}advisor-rules/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
