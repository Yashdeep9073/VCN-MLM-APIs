info:
  name: Delete Assign Permission
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}role-permissions/delete"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"roleId\":2,\r

      \    \"permissionIds\":[1,2]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
