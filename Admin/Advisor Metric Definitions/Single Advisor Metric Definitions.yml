info:
  name: Single Advisor Metric Definitions
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}advisor-metric-definitions/read/3"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
