info:
  name: Single Senior Advisor  Income Definition
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}senior-advisor-income-definitions/read/4"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
