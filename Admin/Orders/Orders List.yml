info:
  name: Orders List
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}orders/read?limit="
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  params:
    - name: limit
      value: ""
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
