info:
  name: Refund Requests
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}orders/read/refund-requests?limit=12"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  params:
    - name: limit
      value: "12"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
