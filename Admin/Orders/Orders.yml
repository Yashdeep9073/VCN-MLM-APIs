info:
  name: Orders
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}orders/read?limit=1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  params:
    - name: limit
      value: "1"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
