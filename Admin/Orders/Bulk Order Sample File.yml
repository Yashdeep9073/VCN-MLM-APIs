info:
  name: Bulk Order Sample File
  type: http
  seq: 9

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}orders/read/bulk-upload/sample"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
