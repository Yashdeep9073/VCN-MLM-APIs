info:
  name: Verify Payment
  type: http
  seq: 2

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}orders/create/verify"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |+
      {
        "razorpay_order_id": "order_T3ToeYleCE3y3k",
        "razorpay_payment_id": "pay_JK23XX8X",
        "razorpay_signature": "89ds8f98sd7f9s8df7...",
        "orderId":1,
        "userId":12
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
