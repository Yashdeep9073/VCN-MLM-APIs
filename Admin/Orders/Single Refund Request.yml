info:
  name: Single Refund Request
  type: http
  seq: 7

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}orders/read/refund-requests/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
