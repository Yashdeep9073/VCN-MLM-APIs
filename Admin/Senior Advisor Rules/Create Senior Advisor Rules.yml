info:
  name: Create Senior Advisor Rules
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}senior-advisor-rules/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "name": "Bronze Senior Advisor",
        "minTeamBV": 50000,
        "minQualifiedAdvisors": 5,
        "minActiveAdvisors": 5,
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
