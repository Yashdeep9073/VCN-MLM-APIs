info:
  name: Full Downline
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}team-genealogy/read/downline/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
