info:
  name: All Child Nodes Single User
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}team-genealogy/read/children/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
