info:
  name: Get Single Report
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}reports/read/123"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
