info:
  name: Get All Reports
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}reports/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
