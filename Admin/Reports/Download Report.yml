info:
  name: Download Report
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}reports/read/2/download"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
