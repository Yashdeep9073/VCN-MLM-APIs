info:
  name: Get Reports
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}reports"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
