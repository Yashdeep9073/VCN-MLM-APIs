info:
  name: Update Expenses Category
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{LOCAL_ADMIN_URL}}expense-category/update"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "id":1,
        "name": "first-4d0",
        "code": "VALUE",
        "description": "hello kse ho app",
       "isActive" : false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
