info:
  name: Expenses Category
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}expense-category/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
