info:
  name: Get List Club Metric Definition
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}club-metric-definitions/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
