info:
  name: Create Club Metric Definition
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}club-metric-definitions/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "code": "BV",
        "name": "Business Volume",
        "description": "Total business volume generated by a member during the qualification period.",
        "dataType": "DECIMAL",
        "unit": "Points",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
