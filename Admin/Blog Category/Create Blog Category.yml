info:
  name: Create Blog Category
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}blog-category/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "name": "first-4d0",
        "slug": "first-sa-3d0",
        "description": "hello kse ho app",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
