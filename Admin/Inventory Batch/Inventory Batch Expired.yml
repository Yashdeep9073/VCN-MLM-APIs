info:
  name: Inventory Batch Expired
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}inventory-batch/read/expired"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
