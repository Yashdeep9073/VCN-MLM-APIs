info:
  name: Single Voucher Campign
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}voucher-campaign/read/2"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
