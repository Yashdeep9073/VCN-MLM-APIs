info:
  name: Update Voucher Campign
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{LOCAL_ADMIN_URL}}voucher-campaign/update"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "id": "1",
        "name": "Summer Sale 2027",
        "endDate": "2026-08-31T23:59:59.000Z",
        "isActive": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
