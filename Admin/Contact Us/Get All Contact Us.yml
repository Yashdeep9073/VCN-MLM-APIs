info:
  name: Get All Contact Us
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}contact-us/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
