info:
  name: Create Units
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}units/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"name\":\"Test\",\r

      \    \"symbol\":\"t\",\r

      \    \"type\":\"WEIGHT\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
