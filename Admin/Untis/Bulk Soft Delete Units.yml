info:
  name: Bulk Soft Delete Units
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}units/delete/soft/bulk"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"unitIds\": [\r

      \        12\r

      \    ]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
