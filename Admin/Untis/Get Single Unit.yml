info:
  name: Get Single Unit
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}units/read/12"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
