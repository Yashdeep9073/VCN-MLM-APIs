info:
  name: All Leads Conversion
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}lead-conversations/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
