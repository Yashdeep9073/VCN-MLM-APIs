info:
  name: Promotion Rules Actions
  type: http
  seq: 8

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}promotion-rule/read/actions/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
