info:
  name: Create Tag
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}tags/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r\n    \"name\":\"Hot Deal\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
