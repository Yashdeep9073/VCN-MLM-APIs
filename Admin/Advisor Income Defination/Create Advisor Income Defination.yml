info:
  name: Create Advisor Income Defination
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}advisor-income-definitions/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "scheduleId": 6,
        "name": "Monthly Leadership Bonus",
        "description": "Bonus distributed every month",
        "periodType": "MONTHLY",
        "customDays": null,
        "requiresQualification": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
