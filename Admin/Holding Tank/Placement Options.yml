info:
  name: Placement Options
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}holding-tank/read/placement-options/2"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
