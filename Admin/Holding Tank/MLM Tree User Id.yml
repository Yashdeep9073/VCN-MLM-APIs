info:
  name: MLM Tree User Id
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}holding-tank/read/mlm-tree-nodes"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
