info:
  name: Holding Tank All Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}holding-tank/read?search="
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  params:
    - name: search
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
