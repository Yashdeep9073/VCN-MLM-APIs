info:
  name: Inventory Product Low Stock
  type: http
  seq: 7

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}inventory/low-stock"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
