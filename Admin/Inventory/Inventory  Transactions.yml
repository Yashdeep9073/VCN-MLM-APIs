info:
  name: Inventory  Transactions
  type: http
  seq: 8

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}inventory/transactions/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
