info:
  name: Create Blog Category Mapping
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}blog-category-mapping/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "blogId":3 ,
        "categoryId":1 
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
