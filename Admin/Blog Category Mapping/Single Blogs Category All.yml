info:
  name: Single Blogs Category All
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}blog-category-mapping/read/blog/3"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
