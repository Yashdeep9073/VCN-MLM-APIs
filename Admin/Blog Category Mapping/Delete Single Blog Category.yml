info:
  name: Delete Single Blog Category
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}blog-category-mapping/delete"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "blogId": "3",
        "categoryId": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
