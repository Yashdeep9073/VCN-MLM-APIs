info:
  name: Bulk Delete Blog Category
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}blog-category-mapping/delete/bulk"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "blogId": "3",
        "categoryIds": [1,2,3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
