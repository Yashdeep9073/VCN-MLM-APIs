info:
  name: Get Snapshots
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}snapshots"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
