info:
  name: Get Single Snapshot Items
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}snapshots/read/121/items"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
