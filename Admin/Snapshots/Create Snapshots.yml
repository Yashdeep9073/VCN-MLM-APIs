info:
  name: Create Snapshots
  type: http
  seq: 1

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}snapshots/"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |
      // Request
      {
          "snapshotType": "BINARY",
          "filters": {
              "userId": "1232324323"
          }
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
