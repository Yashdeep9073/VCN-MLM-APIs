info:
  name: Get Single Snapshot
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}snapshots/read/121"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
