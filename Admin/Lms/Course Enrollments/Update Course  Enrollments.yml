info:
  name: Update Course  Enrollments
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{LOCAL_ADMIN_URL}}lms/enrollments/update"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "id": 1,
        "status": "PAID",
        "progress": 100,
        "completedAt": "2026-08-11T10:30:00.000Z",
        "certificateIssued": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
