info:
  name: Single Courses Questions
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}lms/questions/read/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
