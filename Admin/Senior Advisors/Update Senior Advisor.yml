info:
  name: Update Senior Advisor
  type: http
  seq: 4

http:
  method: PUT
  url: "{{LOCAL_ADMIN_URL}}senior-advisors/update/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "userId": 10,
        "status": "PENDING",
        "areaNames": [
          "North Zone",
          "South Zone",
          "East Zone"
        ],
        "remarks": "Initial area assignment."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
