info:
  name: Create Senior Advisor
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}senior-advisors/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "userId": 15,
        "remarks": "Promoted by admin",
        "areaNames": [
          "Punjab",
          "Chandigarh",
          "Haryana"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
