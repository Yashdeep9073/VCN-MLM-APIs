info:
  name: Get Single Products Tag
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}product-tags/read/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: text
    data: "{\r

      \    \"productId\": 1,\r

      \    \"tagIds\":[1,2,3]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
