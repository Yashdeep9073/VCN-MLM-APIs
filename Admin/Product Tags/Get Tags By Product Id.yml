info:
  name: Get Tags By Product Id
  type: http
  seq: 7

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}product-tags/read/product/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
