info:
  name: Soft Delete Product Tag
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}product-tags/delete/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: text
    data: "{\r

      \    \"productId\": 1,\r

      \    \"tagIds\":[1,2,3]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
