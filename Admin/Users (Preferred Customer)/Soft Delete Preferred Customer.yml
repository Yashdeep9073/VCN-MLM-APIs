info:
  name: Soft Delete Preferred Customer
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}users/preferred-customer/delete/soft/6"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
