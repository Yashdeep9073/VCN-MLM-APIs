info:
  name: All Preferred Customer
  type: http
  seq: 7

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}users/preferred-customer/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
