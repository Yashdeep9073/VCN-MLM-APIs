info:
  name: Get Loyalty Points By User
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}loyalty-points/read/users/search"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
