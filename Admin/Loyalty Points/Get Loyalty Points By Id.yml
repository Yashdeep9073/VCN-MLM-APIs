info:
  name: Get Loyalty Points By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}loyalty-points/read/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
