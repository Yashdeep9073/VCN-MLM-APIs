info:
  name: Create Attributes
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}attributes/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r\n    \"name\":\"Test\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
