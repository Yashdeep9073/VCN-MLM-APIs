info:
  name: Soft Bulk Delete Attributes
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
