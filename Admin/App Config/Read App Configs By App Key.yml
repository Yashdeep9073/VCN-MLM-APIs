info:
  name: Read App Configs By App Key
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}app-configs/read/key/FIRS_DD"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
