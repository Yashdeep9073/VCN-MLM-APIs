info:
  name: Read App Configs By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}app-configs/read/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
