info:
  name: Get All Rank Rewards
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}rank-rewards/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
