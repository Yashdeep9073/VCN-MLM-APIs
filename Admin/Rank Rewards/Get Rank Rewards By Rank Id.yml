info:
  name: Get Rank Rewards By Rank Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}rank-rewards/read/rank/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
