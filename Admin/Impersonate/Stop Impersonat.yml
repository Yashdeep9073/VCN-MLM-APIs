info:
  name: Stop Impersonat
  type: http
  seq: 2

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}impersonate/stop"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "sessionId":14
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
