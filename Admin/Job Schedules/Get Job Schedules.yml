info:
  name: Get Job Schedules
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}job-schedules/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
