info:
  name: Single Recycle Bin Data
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}recycle-bin/read/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
