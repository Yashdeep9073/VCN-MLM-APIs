info:
  name: Restore
  type: http
  seq: 2

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}recycle-bin/restore/4"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
