info:
  name: Restore Bulk
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}recycle-bin/restore/bulk"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \  \"recycleBinIds\": [\r

      \    1\r

      \  ]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
