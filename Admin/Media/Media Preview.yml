info:
  name: Media Preview
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}media/preview/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
