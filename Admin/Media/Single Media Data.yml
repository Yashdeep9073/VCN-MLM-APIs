info:
  name: Single Media Data
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}media/read/4"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
