info:
  name: "Crreate Media Bulk "
  type: http
  seq: 5

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}media/create/bulk-upload"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/yashdeep/Downloads/Alpha Care-20260609T081529Z-3-001.zip
      - name: folder
        type: text
        value: DOCUMENT
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
