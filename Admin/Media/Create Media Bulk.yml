info:
  name: "Create Media Bulk "
  type: http
  seq: 5

http:
  method: POST
  url: "{{SERVER_ADMIN_URL}}media/create/bulk-upload"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: multipart-form
    data:
      - name: folder
        type: text
        value: PRODUCT
      - name: file
        type: file
        value:
          - /home/yashdeep/Downloads/epicalepisodes_vcn-e-commerce_2026-06-30_0711.zip
        contentType: application/zip
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
