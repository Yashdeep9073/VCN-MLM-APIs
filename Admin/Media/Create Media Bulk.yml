info:
  name: "Create Media Bulk "
  type: http
  seq: 5

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}media/create/bulk-upload"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: multipart-form
    data:
      - name: folder
        type: text
        value: PRODUCT
      - name: file
        type: file
        value:
          - /home/yashdeep/Downloads/Alpha Care-20260609T081529Z-3-001.zip
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
