info:
  name: Direct Seller Roles
  type: http
  seq: 7

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}roles/read/direct-seller"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
