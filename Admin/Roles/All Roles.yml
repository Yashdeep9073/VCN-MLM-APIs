info:
  name: All Roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}roles/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
