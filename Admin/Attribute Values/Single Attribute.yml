info:
  name: Single Attribute
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}attribute-values/read/20"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
