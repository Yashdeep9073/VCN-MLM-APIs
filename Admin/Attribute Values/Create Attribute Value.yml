info:
  name: Create Attribute Value
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}attribute-values/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"attributeId\": 6,\r

      \    \"value\":\"sm\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
