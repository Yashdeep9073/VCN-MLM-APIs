info:
  name: Delete Product Image
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}product-images/delete/hard/3"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
