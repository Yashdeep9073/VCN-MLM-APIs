info:
  name: All Product Images
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}product-images/read/10"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
