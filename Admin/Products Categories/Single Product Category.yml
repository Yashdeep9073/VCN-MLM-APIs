info:
  name: Single Product Category
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}product-categories/read/9"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
