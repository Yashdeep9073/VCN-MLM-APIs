info:
  name: Create Product Category
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}product-categories/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"name\":\"Joint Care\",\r

      \    \"imageId\":1\r

      \r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
