info:
  name: Delete Ticket
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}tickets/delete/2"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
