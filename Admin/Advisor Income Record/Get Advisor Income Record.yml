info:
  name: Get Advisor Income Record
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}advisor-income-records/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
