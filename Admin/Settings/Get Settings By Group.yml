info:
  name: Get Settings By Group
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}settings/read/grouped"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
