info:
  name: Get Settings By Key
  type: http
  seq: 7

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}settings/read/key/SNA_1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
