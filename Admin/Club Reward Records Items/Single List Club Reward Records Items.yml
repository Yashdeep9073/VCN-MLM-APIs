info:
  name: Single List Club Reward Records Items
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}club-reward-record-items/read/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
