info:
  name: Update Club Reward Components
  type: http
  seq: 4

http:
  method: PUT
  url: "{{LOCAL_ADMIN_URL}}club-reward-components/update/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "rewardDefinitionId": 1,
        "walletId": 1,
        "calculationType": "FIXED",
        "fixedAmount": 1000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
