info:
  name: Delete Bulk Clubs
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}clubs/delete/soft/bulk"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "clubIds":[12312]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
