info:
  name: Create Club Qualification Rules
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}club-qualification-rules/create"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "clubId": 1,
        "metricId": 2,
        "operator": "GREATER_THAN_EQUAL",
        "targetValue": 1000,
        "sequence": 1,
        "isRequired": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
