info:
  name: Single Admin User
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}admin-users/read/5"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
