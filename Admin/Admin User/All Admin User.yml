info:
  name: All Admin User
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}admin-users/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
