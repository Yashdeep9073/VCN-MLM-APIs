info:
  name: Single Deal
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}deals/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
