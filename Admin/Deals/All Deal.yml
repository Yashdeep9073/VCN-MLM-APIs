info:
  name: All Deal
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}deals"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
