info:
  name: Wallet Payout Setting By Wallet Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}wallet-payout-settings/read/wallet/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
