info:
  name: User Assign Wallet
  type: http
  seq: 9

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}wallet/user-wallet/assign"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r

      \    \"userId\": 16,\r

      \    \"walletId\": 5\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
