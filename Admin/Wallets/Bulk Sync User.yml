info:
  name: Bulk Sync User
  type: http
  seq: 12

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}wallet/user-wallet/sync-user/bulk"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r\n    \"userIds\": [7]\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
