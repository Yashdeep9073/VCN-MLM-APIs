info:
  name: Single User Wallet
  type: http
  seq: 7

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}wallet/read/single-user/302"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
