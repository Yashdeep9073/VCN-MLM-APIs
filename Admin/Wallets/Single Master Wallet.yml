info:
  name: Single Master Wallet
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}wallet/read/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
