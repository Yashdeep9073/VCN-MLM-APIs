info:
  name: Pending Earnings
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}financial/read/pending-earnings"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
