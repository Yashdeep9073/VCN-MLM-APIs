info:
  name: Bv Growth
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}financial/read/chart/bv-growth"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
