info:
  name: Leg Volume
  type: http
  seq: 8

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}financial/read/chart/leg-volume"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
