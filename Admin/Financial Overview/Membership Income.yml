info:
  name: Membership Income
  type: http
  seq: 7

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}financial/read/chart/membership-income"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
