info:
  name: Club Income
  type: http
  seq: 9

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}financial/read/chart/club-income"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
