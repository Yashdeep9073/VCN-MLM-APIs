info:
  name: Get Financial Overview
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}financial/read/overview"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
