info:
  name: All Direct Seller
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}users/direct-seller/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
