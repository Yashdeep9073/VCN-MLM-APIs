info:
  name: Make Defaulter
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_ADMIN_URL}}users/direct-seller/update/defaulter/13324234"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
