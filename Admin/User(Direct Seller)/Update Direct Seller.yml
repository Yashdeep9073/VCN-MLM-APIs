info:
  name: Update Direct Seller
  type: http
  seq: 1

http:
  method: PUT
  url: "{{LOCAL_ADMIN_URL}}users/direct-seller/update"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
          "id":1,
          "password":"kelton7227",
          "confirmPassword":"kelton7227"
      //     "profileImageId":2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
