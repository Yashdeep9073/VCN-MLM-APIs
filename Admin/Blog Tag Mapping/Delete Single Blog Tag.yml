info:
  name: Delete Single Blog Tag
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}blog-tag-mapping/delete"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "blogId": "3",
        "tagId": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
