info:
  name: Read Blog Tag Mapping
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}blog-tag-mapping/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
