info:
  name: Update Senior Advisor Qualifications Rules
  type: http
  seq: 4

http:
  method: PUT
  url: "{{LOCAL_ADMIN_URL}}senior-advisor-qualification-rules/update/5"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "definitionId": 1,
        "metricId": 4,
        "operator": "GREATER_THAN_EQUAL",
        "targetValue": 10000,
        "sequence": 1,
        "isRequired": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
