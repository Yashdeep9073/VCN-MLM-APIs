info:
  name: Get Senior Advisors Qualifications Rules
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}senior-advisor-qualification-rules/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
