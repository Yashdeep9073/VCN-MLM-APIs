info:
  name: Get All Business Center
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}business-centre/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
