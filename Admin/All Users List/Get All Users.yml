info:
  name: Get All Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}all-users/read?search=torrance9390"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  params:
    - name: search
      value: torrance9390
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
