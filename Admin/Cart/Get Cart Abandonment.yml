info:
  name: Get Cart Abandonment
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}cart/abandonment"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
