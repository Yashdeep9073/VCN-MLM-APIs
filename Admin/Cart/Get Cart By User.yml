info:
  name: Get Cart By User
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}cart/read/user/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
