info:
  name: Ecommerce Dashboard
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}dashboard/read/ecommerce-dashboard"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
