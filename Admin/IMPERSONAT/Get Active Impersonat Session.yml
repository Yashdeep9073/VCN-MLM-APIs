info:
  name: Get Active Impersonat Session
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}impersonate/active"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
