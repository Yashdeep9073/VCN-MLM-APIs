info:
  name: Update Integration Settings
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{LOCAL_ADMIN_URL}}integration-settings/update"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "id":1,
        "type": "PAYMENT",
        "provider": "fao f,afha",
        "config": {
          "hello": "jhel",
          "val": "jhel"
        },
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
