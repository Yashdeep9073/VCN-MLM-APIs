info:
  name: All Wallet Charges Against Wallet
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}wallet-charge/read/wallet/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
