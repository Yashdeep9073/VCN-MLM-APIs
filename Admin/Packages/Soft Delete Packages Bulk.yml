info:
  name: Soft Delete Packages Bulk
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}packages/delete/soft/bulk"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r\n    \"packageIds\":[12]\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
