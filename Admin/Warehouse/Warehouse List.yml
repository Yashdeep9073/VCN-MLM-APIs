info:
  name: Warehouse List
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_ADMIN_URL}}warehouse/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
