info:
  name: Get CMS Section Items
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}cms-section-items/read"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
