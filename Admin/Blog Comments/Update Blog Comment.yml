info:
  name: Update Blog Comment
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{LOCAL_ADMIN_URL}}blog-comment/update"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |+
      {
       "id":1,
        "comment":"akfha ofoajflaj faof aofjaoj",
        "isApproved":true
        
      }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
