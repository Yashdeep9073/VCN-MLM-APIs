info:
  name: Get Binary Flush Data
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}binary-flush/read?userName=Founder01"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  params:
    - name: userName
      value: Founder01
      type: query
  body:
    type: json
    data: "\n"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
