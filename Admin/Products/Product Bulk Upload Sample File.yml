info:
  name: Product Bulk Upload Sample File
  type: http
  seq: 7

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}products/read/bulk-upload/sample"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
