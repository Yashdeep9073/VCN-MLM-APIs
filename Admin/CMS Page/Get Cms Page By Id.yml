info:
  name: Get Cms Page By Id
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_ADMIN_URL}}cms-pages/read/11"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
