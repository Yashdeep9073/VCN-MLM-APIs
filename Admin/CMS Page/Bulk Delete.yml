info:
  name: Bulk Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{LOCAL_ADMIN_URL}}cms-pages/delete/soft/bulk"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: "{\r\n    \"pageIds\":[5]\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
